# boundary

this gift is the dialect. complete for that job.

## ships

| | |
|---|---|
| language | topic-comment english. japanese grammar shape. marks. iron and slack. scale. think + write + traces |
| architecture | genome / warehouse / canon |
| speech acts | `ERROR` · `DONT_KNOW` · trailing `please` · `etc` as open class |
| hygiene | machine-checkable tells. linter owns the list so agents do not recite it |
| prompts | drop-in genome and canon. warehouse is per project |
| tools | parse · lint · prompt emit · conformance |
| license | AGPL-3.0-or-later. covenant in `COVENANT.md` |

`docs/SPEC.md` is the law. tools fail closed against it.

## stays off this repo

house life. care. money books. named house rituals. product genomes. mill control language. patent dictionary. private pools.

those have other homes. they are not required to implement progen. if a patch drags them in, reject the patch.

## completeness claim

an implementer who has only this repository can wire the dialect into an agent desk, a product glass, or an OS shell. named sources of truth in SPEC are files in this tree. no private path is load-bearing.
